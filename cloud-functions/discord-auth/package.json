{
  "name": "discord-auth-cloud-function",
  "version": "1.0.0",
  "description": "Google Cloud Function pour Discord OAuth2",
  "main": "index.js",
  "scripts": {
    "start": "functions-framework --target=discordAuth --port=8080",
    "deploy": "gcloud functions deploy discord-auth --runtime nodejs20 --trigger-http --allow-unauthenticated --entry-point discordAuth"
  },
  "dependencies": {
    "@google-cloud/functions-framework": "^3.3.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}

